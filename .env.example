# Optional. Only used by Trendix web container — never by Dolfino.
# Leave empty to use the built-in Tor proxy (trendix_proxy:8118).
PROXY_URL=

# MetaTrader autotrade (local PC only — never commit real passwords)
# Prefer entering these in the UI page «معامله خودکار».
# MT5_LOGIN=
# MT5_PASSWORD=
# MT5_SERVER=Alpari-MT5-Demo
